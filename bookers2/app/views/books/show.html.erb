<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
    <%= flash[:success] %>
    <%= flash[:danger] %>
    <h2>User info</h2>
    <%= attachment_image_tag @user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg"%>
    <table class="table">
        <thead>
          <tr>
            <th>name</th>
            <td><%= @user.name %></td>
          </tr>
        </thead>
        <tbody>
          <tr>
            <th>introduction</th>
            <td><%= @user.introduction %></td>
          </tr>
        </tbody>
      </table>
    <div class="row">
      <button type="button" class="btn btn-outline-secondary btn-block fas fa-user-cog">
   <%# if @book.user.id == current_user.id %>
      <%= link_to "プロフィール編集", edit_user_path(@user) %>
  <%# end %>
      </button>
    </div>

<% if @new_book.errors.any? %>
  <p><%= @new_book.errors.count %>件のエラーが発生しました</p>
  <% @new_bookk.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>

  <h2>New book</h2>
  <div class="form">
   <%= form_with model:@new_book, local:true do |f| %>
    <br><label for="inputTitle">title</label></br>
    <%= f.text_field :title %>
    <br><label for="inputBody">Opinion</label></br>
    <%= f.text_area :body %>

   <%= f.submit "Create Book" %>
 <% end %>
</div>
</div>

    <div class="col-md-8 offset-md-1">
      <h2>Book detail</h2>

      <table class="table table-hover table-inverse">
         <thead>
           <tr>
            <th></th>
            <th>Title</th>
            <th>Opinion</th>
            <th></th>
            <th></th>
           </tr>
         </thead>
         <tbody>
           <tr>
           <th>
           <%= link_to user_path(@user) do %>
           <%= attachment_image_tag @user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg"%>
           <br><%= @user.name %></br>
           <% end %>
           </th>
           <th><%= @book.title %></th>
           <th><%= @book.body %></th>
          <% if @user.id == current_user.id %>
           <th><%= link_to "Edit", edit_book_path(@book), class:"btn btn-sm btn-success" %></th>
           <th><%= link_to "Destroy", book_path(), class:"btn btn-sm btn-danger", method: :delete, "data-confirm" => "本当に削除しますか？" %></th>
          <% end %>
          </tr>
         </tbody>
      </table>
    </div>
  </div>
</div>



</div>


</div>

